{
    "routes": [
        {
            "route": "/proxy/*",
            "rewrite": "https://samaan-api.azurewebsites.net/api/*"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
            "/images/*.{png,jpg,gif,svg,ico}",
            "/_next/*",
            "/proxy/*"
        ]
    }
}